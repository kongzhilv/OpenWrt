config netdata_clients 'main'
	option enabled '1'
	option lan_dev 'br-lan'
	# 可选刷新间隔：1、3、5、10 秒。默认 3 秒，越小 CPU 开销越高。
	option update_every '3'
	# 重新扫描在线客户端的间隔，最低 30 秒。
	option rescan_every '60'
	# 最多显示客户端数量，防止规则和维度过多。
	option max_clients '64'
