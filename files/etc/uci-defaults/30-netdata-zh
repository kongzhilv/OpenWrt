#!/bin/sh

logger -t netdata-zh "skip Netdata static web asset translation"

# Intentionally do not rewrite /usr/share/netdata/web.
# Netdata frontend assets change across versions and global sed replacement can
# corrupt JavaScript, CSS or JSON keys. Keep the official Netdata UI intact.
# Custom OpenWrt charts should provide their own Chinese chart titles instead.

logger -t netdata-zh "done"
exit 0
